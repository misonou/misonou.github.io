<Module name="brew-js-react" />

# isViewRendered

Returns whether a registered view component is currently being rendered.

## Syntax

```javascript
isViewRendered(view)
```

<ImportHint
    name="isViewRendered"
    module="brew-js-react"
    require="isViewRendered"
    global="brew.react.isViewRendered" />

### Parameters

<dl>
    #### `view`
    A view component returned from [`registerView`](:).

</dl>

## Version information

<VersionTimeline module="brew-js-react">
- **0.5.5** Introduced
</VersionTimeline>
