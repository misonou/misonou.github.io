<Module name="@misonou/react-app-utils" />

# HTMLConsole class

> For usage see [In-page console](/docs/devtools/html-console).

<MemberList
    i="HTMLConsole"
    ip={['element']}
    im={['log()', 'warn()', 'error()', 'clear()']}
/>

## Constructor

```javascript
new HTMLConsole(element)
```

### Parameters

<dl>
    #### `element`
</dl>

## Instance properties

### `HTMLConsole.element` <Badge.ReadOnly />

## Instance methods

### `HTMLConsole.log()`

```javascript
log()
log(args0)
log(args0, args1)
log(args0, args1, /* … ,*/ argsN)
```

### Parameters

<dl>
    #### `args0` … `argsN` <Badge.Optional />
</dl>

### `HTMLConsole.warn()`

```javascript
warn()
warn(args0)
warn(args0, args1)
warn(args0, args1, /* … ,*/ argsN)
```

### Parameters

<dl>
    #### `args0` … `argsN` <Badge.Optional />
</dl>

### `HTMLConsole.error()`

```javascript
error()
error(args0)
error(args0, args1)
error(args0, args1, /* … ,*/ argsN)
```

### Parameters

<dl>
    #### `args0` … `argsN` <Badge.Optional />
</dl>

### `HTMLConsole.clear()`

```javascript
clear()
```
