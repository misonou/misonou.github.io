<Module name="zeta-dom/tree" />

# NodeTree class

<MemberList
    i="NodeTree"
    ip={['element', 'rootNode']}
    im={['getNode()', 'setNode()', 'removeNode()', 'update()', 'on()']}
    ev={['update']}
/>

## Instance properties

### `NodeTree.element` <Badge.ReadOnly />

### `NodeTree.rootNode` <Badge.ReadOnly />

## Instance methods

### `NodeTree.getNode()`

```javascript
getNode(element)
```

### Parameters

<dl>
    #### `element`
</dl>

### `NodeTree.on()`

```javascript
on(handlers)
on(event, handler)
```

### Parameters

<dl>
    #### `handlers`
    #### `event`
    #### `handler`
</dl>

### `NodeTree.removeNode()`

```javascript
removeNode(node)
```

### Parameters

<dl>
    #### `node`
</dl>

### `NodeTree.setNode()`

```javascript
setNode(element)
```

### Parameters

<dl>
    #### `element`
</dl>

### `NodeTree.update()`

```javascript
update()
```

## Events

### `update` event
