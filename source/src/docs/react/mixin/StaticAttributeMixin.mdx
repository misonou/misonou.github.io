<Module name="brew-js-react" />

# StaticAttributeMixin class

<ImportHint
    name="StaticAttributeMixin"
    module="brew-js-react"
    require="StaticAttributeMixin"
    global="brew.react.StaticAttributeMixin"
    expanded />

<MemberList
    i="StaticAttributeMixin"
    extends={['Mixin']}
/>

## Constructor

```javascript
new StaticAttributeMixin(name, value)
new StaticAttributeMixin(attributes)
```

### Parameters

<dl>
    #### `name`
    Name of attribute.

    #### `attributes`
    A dictionary containing attributes and their values to be set on rendered elements.

    #### `value`
    Value to be set. Default is empty string.

</dl>

## Version infomation

<VersionTimeline>
- **0.1.0** Introduced
- **0.5.3** Overload with `name` and `value` parameters
</VersionTimeline>
