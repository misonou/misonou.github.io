import PersistExample from "src/components/examples/form-persist";

<Module name="zeta-dom-react" />

# Persisting form in history

It is possible to persist form data in history so that previous data is restored when user navigated back.

> Note that restored data, if any, will override initial data passed to [`useFormContext`](:).

<PersistExample />
