import BasicExample from "src/components/examples/form-initial-data";

<Module name="zeta-dom-react" />

# Initial form data

It is possible to render form with initial data by passing it to [`useFormContext`](:).

> When initial data is supplied, [`FormContext.reset()`](:) will reset all fields to initial data while [`FormContext.clear()`](:) will clear all fields.

<BasicExample />
