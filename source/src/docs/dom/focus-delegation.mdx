import Example from "src/components/examples/focus-delegation";

<Module name="zeta-dom/dom" />

# Focus delegation

Focus delegation is much like portal in React, where focus and events are delegated to
another element which is not a descandant of a given element.

The most common use case is for a flyout or dialog.

Notice how the first box is kept in focus state while clicking the second box in the following example.

<Example />
